setup:
  - apt-get update && apt-get install -y curl gnupg
  - curl -fsSL https://deb.nodesource.com/setup_18.x | bash -
  - apt-get install -y nodejs
  - npm ci
  - npx playwright install --with-deps
workdir: /workspace/rat-whisperer-journal-app
run: npm test
